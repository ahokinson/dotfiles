_: {
  networking.networkmanager.enable = true;
  # Per-chip workarounds live with the host that has the chip: framework13's
  # MT7925 opt-out in its default.nix, the Asahi backend in asahi-common.nix.
}
